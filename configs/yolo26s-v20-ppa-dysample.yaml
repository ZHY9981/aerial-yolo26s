# YOLO26s V20 — V16 + P5 256ch CoordAtt (clean third head)
# ====================================================
# Changes (vs V16.0):
#   1. Added P5 256ch detection head (removed the previous V20's DySample/PPA/C2f-DD)
#   2. Added CoordAtt[32] before the P5 detection head
#
# Design rationale:
#   V16 (dual-head): 74.0% — person=60.2%, cycle=42.9%, car=67.3%
#   Old V20 (triple-head + PPA + DySample + C2f-DD): 73.21% — polluted by multiple modules
#   New V20 adds P5 in the cleanest possible way, answering:
#   "Can P5 256ch + CoordAtt recover car while preserving person/cycle?"
#
# Inherits V16:
#   - TAL stride=4px, class weight person×2/cycle×3 (in loss.py)
#   - WIoU v3, scale=0.3, close_mosaic=10
#   - CoordAtt placed independently before each detection head
#   - nearest upsampling (no DySample)

# Parameters
nc: 7
end2end: True
reg_max: 1
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# Backbone — identical to V16
backbone:
  - [-1, 1, Conv, [64, 3, 2]]           # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]          # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]   # 2
  - [-1, 1, Conv, [256, 3, 2]]          # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]   # 4
  - [-1, 1, Conv, [512, 3, 2]]          # 5-P4/16
  - [-1, 2, C3k2, [512, True]]          # 6
  - [-1, 1, Conv, [1024, 3, 2]]         # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]         # 8
  - [-1, 1, SPPF, [1024, 5, 3, True]]   # 9
  - [-1, 2, C2PSA, [1024]]              # 10

# Head — V16's P3+P4 + new P5 256ch detection head
head:
  # --- FPN: P5→P4 ---
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 11
  - [[-1, 6], 1, Concat, [1]]                     # 12 cat backbone P4
  - [-1, 2, C3k2, [512, True]]                    # 13 (P4/16-FPN)

  # --- FPN: P4→P3 ---
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 14
  - [[-1, 4], 1, Concat, [1]]                     # 15 cat backbone P3
  - [-1, 2, C3k2, [256, True]]                    # 16 (P3/8-FPN)
  - [-1, 1, CoordAtt, [32]]                       # 17 ⭐ P3 CoordAtt

  # --- PAN: P3→P4 ---
  - [-1, 1, Conv, [256, 3, 2]]                    # 18
  - [[-1, 13], 1, Concat, [1]]                    # 19 cat FPN P4
  - [-1, 2, C3k2, [512, True]]                    # 20 (P4/16-PAN)
  - [-1, 1, CoordAtt, [32]]                       # 21 ⭐ P4 CoordAtt

  # --- NEW PAN: P4→P5 (256ch full version) ---
  - [-1, 1, Conv, [256, 3, 2]]                    # 22 P4→P5 down (512→256ch)
  - [[-1, 10], 1, Concat, [1]]                    # 23 concat backbone P5(1024ch)
  - [-1, 2, C3k2, [512, True]]                    # 24 (P5/32-PAN fusion)
  - [-1, 1, CoordAtt, [32]]                       # 25 ⭐ P5 CoordAtt

  # --- Detect — triple-head ---
  - [[17, 21, 25], 1, Detect, [nc]]               # 26 P3+P4+P5 triple-head
