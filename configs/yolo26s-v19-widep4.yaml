# YOLO26s V19 — V17 + P4 wide channel 256→384
# ====================================================
# Changes (vs V17):
#   P4 neck output channels 256→384
#   Hypothesis: P4 carries car/truck/bus/freight (4 classes) simultaneously;
#   256ch may be insufficient.
#   VRAM: +~600MB (within 3.5GB headroom)
#
# P3 unchanged, person/cycle unaffected.
# Inherits V17: imgsz=800, batch=4, epochs=120, CoordAtt, RepNCSPELAN4, TAL 4px

# Parameters
nc: 7
end2end: True
reg_max: 1
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

backbone:
  - [-1, 1, Conv, [64, 3, 2]]          # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]         # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]   # 2
  - [-1, 1, Conv, [256, 3, 2]]         # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]   # 4
  - [-1, 1, Conv, [512, 3, 2]]         # 5-P4/16
  - [-1, 2, C3k2, [512, True]]          # 6
  - [-1, 1, Conv, [1024, 3, 2]]        # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]         # 8
  - [-1, 1, SPPF, [1024, 5, 3, True]]   # 9
  - [-1, 2, C2PSA, [1024]]              # 10

head:
  # FPN: P5→P4
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]     # 11
  - [[-1, 6], 1, Concat, [1]]                        # 12
  - [-1, 2, C3k2, [512, True]]                       # 13 (P4/16-FPN)

  # FPN: P4→P3
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]      # 14
  - [[-1, 4], 1, Concat, [1]]                        # 15
  - [-1, 2, C3k2, [256, True]]                       # 16 (P3/8-small)
  - [-1, 1, CoordAtt, [32]]                          # 17 P3 CoordAtt

  # PAN: P3→P4
  - [-1, 1, Conv, [256, 3, 2]]                       # 18
  - [[-1, 13], 1, Concat, [1]]                        # 19
  - [-1, 2, C3k2, [768, True]]                       # 20 ⭐ P4 PAN: 768→384ch
  - [-1, 1, RepNCSPELAN4, [384, 192, 96, 1]]        # 21 ⭐ P4 RepNCSPELAN4 384ch
  - [-1, 1, CoordAtt, [32]]                          # 22 P4 CoordAtt 384ch

  # Detect
  - [[17, 22], 1, Detect, [nc]]                       # 23 P3(128ch)+P4(384ch) dual-head
